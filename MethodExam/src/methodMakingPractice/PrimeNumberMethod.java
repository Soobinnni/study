package methodMakingPractice;

public class PrimeNumberMethod {

		//50개의 소수를 10개씩 한 행으로 출력하는 프로그램
	
	public static void main(String[] args) {
	//출력 메서드
		System.out.println("50개의 소수를 출력하는 프로그램");
		//printPrimeNumbers(50);
	}
	
	/*
	 * public static void printPrimeNumbers(int numberOfPrimes) { final int
	 * NUMBER_OF_PRIMES_PER_LINE = 10; int count = 0; int number = 2; //소수의 테스트 초기
	 * 숫자를 2로 설정한다.
	 * 
	 * //소수 찾기 while(count<numberOfPrimes) { if(isPrime(number)) { if(count %
	 * NUMBER_OF_PRIMES_PER_LINE ==0) { System.out.printf("%-5s", number); } else {
	 * System.out.printf("%-5s", number); } } else {
	 * 
	 * } } }
	 * 
	 * public static boolean isPrime(int number) { //소수를 판단하는 메서드
	 * 
	 * for(int divisor=2;divisor<=(number/2);divisor++) { if(number % divisor == 0 )
	 * { return false; } else { return true; } } }
	 */
	
}
